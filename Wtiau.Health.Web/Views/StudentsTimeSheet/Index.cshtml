@model IEnumerable<Wtiau.Health.Web.Models.ViewModels.Model_StudentTakeTimeList>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}







<div class="panel panel-body panel-default  navigation">

    <div class="pull-left">
        <h4 class="navigation-title"><b>تایم های کاری</b></h4>
    </div>

    <div class="pull-right">
        <a href="@Url.Action("Excel","StudentsTimeSheet", new { id = ViewBag.ID })" ><i class="glyphicon glyphicon-cloud-download navigation-btn-icon"></i></a>
    </div>

</div>

<div class="panel panel-default">

    <div class="panel-body table-body" style="overflow-x:auto;">

        <table id="DateTable" class="table  table-bordered table-striped table-secondary" style="width:100%;">

            <thead class="thead-dark">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.Student_Code)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.Student_National)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.Student_Name)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.Student_Family)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.per)</th>
                    <th scope="col" data-orderable="false">کنترل</th>
                </tr>
            </thead>

            <tbody>
                @{
                    int x = 0;
                }

                @foreach (var item in Model)
                {
                    x++;

                    <tr>
                        <td scope="row">@x</td>

                        <td scope="row">
                            <span> @Html.DisplayFor(modelItem => item.Student_Code) </span>
                        </td>

                        <td scope="row">
                            <span> @Html.DisplayFor(modelItem => item.Student_National) </span>
                        </td>

                        <td scope="row">
                            <span> @Html.DisplayFor(modelItem => item.Student_Name) </span>
                        </td>

                        <td scope="row">
                            <span> @Html.DisplayFor(modelItem => item.Student_Family) </span>
                        </td>

                        <td scope="row">
                            <span> @Html.DisplayFor(modelItem => item.per) </span>
                        </td>

                        <td scope="row">
                            <a class="btn btn-info btn-sm glyphicon glyphicon-list" href="@Url.Action("Index", "TimeSheet", new { id = item.ID })"> </a>
                            <a class="btn btn-warning btn-sm glyphicon glyphicon-pencil" href="javascript:void(0)" onclick="Edit(@item.ID)"> </a>
                            <a class="btn btn-danger btn-sm glyphicon glyphicon-trash" href="javascript:void(0)" onclick="Delete(@item.ID)"> </a>
                        </td>
                    </tr>
                }

            </tbody>

        </table>

    </div>

</div>


@Html.Partial("_Modal")

@section Modal {

    <script>

        function Delete(id) {
            $.ajax({
                url: "/Turn/TurnDelete/" + id,
                type: "Get",
                data: {}
            }).done(function (result) {
                $('#myModal').modal('show');
                $('#myModalLabel').html('حدف تایم');
                $('#myModalBody').html(result);
            });
        }

        function Create() {
            $.ajax({
                url: "/Turn/TurnAdd/",
                type: "Get",
                data: {}
            }).done(function (result) {
                $('#myModal').modal('show');
                $('#myModalLabel').html('افزودن تایم');
                $('#myModalBody').html(result);
            });
        }

        function Edit(id) {
            $.ajax({
                url: "/Turn/TurnEdit/" + id,
                type: "Get",
                data: {}
            }).done(function (result) {
                $('#myModal').modal('show');
                $('#myModalLabel').html('ویرایش تایم');
                $('#myModalBody').html(result);
            });
        }

    </script>

}



